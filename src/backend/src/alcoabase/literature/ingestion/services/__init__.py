"""Ingestion pipeline business logic services.

This sub-package contains the core services for the ingestion pipeline,
including the state machine, storage manager, ingestion orchestrator,
and sanitization pipeline.
"""
