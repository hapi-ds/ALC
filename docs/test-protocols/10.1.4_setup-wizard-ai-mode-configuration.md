# Test Protocol 10.1.4 — Setup Wizard: AI Mode Configuration

| Field | Value |
|-------|-------|
| **Protocol ID** | TP-10.1.4 |
| **Title** | Setup Wizard — AI Mode Configuration |
| **Version** | 1.0 |
| **Date Created** | 2026-06-13 |
| **Author** | hk |
| **Reviewer** | |
| **Objective** | Verify all 3 AI modes (mock, gpu, cpu) can be configured, vLLM service status reflects the selected mode, and AI mode can be switched post-setup |

---

## Prerequisites

| # | Prerequisite | Verified |
|---|-------------|----------|
| P1 | TP-10.1.2 passed (setup wizard completed, admin login works) | [ ] |
| P2 | Backend accessible at http://localhost:8080 | [ ] |
| P3 | Frontend accessible at http://localhost:3000 | [ ] |
| P4 | Admin auth token available | [ ] |

---

## Test Environment

| Parameter | Value |
|-----------|-------|
| OS | |
| Backend URL | http://localhost:8080 |
| Frontend URL | http://localhost:3000 |
| Initial AI Mode | mock (from make setup) |
| GPU Available | yes / no |

---

## Test Steps

### Step 1: Get Auth Token

| Field | Detail |
|-------|--------|
| **Action** | Login as admin and store access token |
| **Command** | `export TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"SecureP@ss2024!"}' \| python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")` |
| **Expected Result** | TOKEN variable set. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 2: Verify Current AI Mode is "mock"

| Field | Detail |
|-------|--------|
| **Action** | Check the system configuration for the current AI mode |
| **Command** | `curl -s http://localhost:8080/api/system-config/ai -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" \| python3 -m json.tool` |
| **Expected Result** | JSON response containing current AI mode: `"mode": "mock"` or similar. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 3: Verify vLLM Service Status in Mock Mode

| Field | Detail |
|-------|--------|
| **Action** | Check vLLM status — should show as unreachable/disabled in mock mode |
| **Command** | `curl -s http://localhost:8080/api/system-config/vllm/status -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" \| python3 -m json.tool` |
| **Expected Result** | Status: "unreachable" or "mock" — vLLM is not running in mock mode. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 4: Verify Health Check Shows vLLM as Unreachable

| Field | Detail |
|-------|--------|
| **Action** | Check the health status endpoint for vLLM service |
| **Command** | `curl -s http://localhost:8080/api/system-config/health/status -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" \| python3 -c "import sys,json; data=json.load(sys.stdin); vllm=[s for s in data if s['service_name']=='vllm'][0]; print(f'vLLM: {vllm[\"status\"]}')"` |
| **Expected Result** | `vLLM: unreachable` — expected in mock mode since no vLLM container runs. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 5: Verify AI Inference Returns Mock Responses

| Field | Detail |
|-------|--------|
| **Action** | Test that AI endpoints return deterministic mock responses (not actual LLM output) |
| **Command** | `curl -s -X POST http://localhost:8080/api/knowledge/ask -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" -H "Content-Type: application/json" -H "X-Change-Reason: Test AI mock" -d '{"question":"What is GMP?"}' \| python3 -m json.tool` |
| **Expected Result** | Returns a response (mock content). Should not timeout or return connection error. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 6: Switch AI Mode to "cpu" via API

| Field | Detail |
|-------|--------|
| **Action** | Change the AI mode configuration to CPU mode |
| **Command** | `curl -s -X PUT http://localhost:8080/api/system-config/ai -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" -H "Content-Type: application/json" -H "X-Change-Reason: Testing AI mode switch to cpu" -d '{"mode":"cpu"}'` |
| **Expected Result** | HTTP 200 with updated configuration showing `"mode": "cpu"`. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 7: Verify AI Mode Persisted to "cpu"

| Field | Detail |
|-------|--------|
| **Action** | Re-read AI configuration to confirm mode change persisted |
| **Command** | `curl -s http://localhost:8080/api/system-config/ai -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" \| python3 -m json.tool` |
| **Expected Result** | `"mode": "cpu"` in response. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 8: Switch AI Mode to "gpu" via API

| Field | Detail |
|-------|--------|
| **Action** | Change the AI mode configuration to GPU mode |
| **Command** | `curl -s -X PUT http://localhost:8080/api/system-config/ai -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" -H "Content-Type: application/json" -H "X-Change-Reason: Testing AI mode switch to gpu" -d '{"mode":"gpu"}'` |
| **Expected Result** | HTTP 200 with updated configuration showing `"mode": "gpu"`. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 9: Switch Back to "mock" Mode

| Field | Detail |
|-------|--------|
| **Action** | Reset AI mode back to mock for remaining tests |
| **Command** | `curl -s -X PUT http://localhost:8080/api/system-config/ai -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" -H "Content-Type: application/json" -H "X-Change-Reason: Reset to mock mode after testing" -d '{"mode":"mock"}'` |
| **Expected Result** | HTTP 200 with `"mode": "mock"`. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 10: Verify Invalid AI Mode Rejected

| Field | Detail |
|-------|--------|
| **Action** | Attempt to set an invalid AI mode — should be rejected |
| **Command** | `curl -s -X PUT http://localhost:8080/api/system-config/ai -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" -H "Content-Type: application/json" -H "X-Change-Reason: Invalid mode test" -d '{"mode":"invalid_mode"}'` |
| **Expected Result** | HTTP 400/422 with validation error. Mode not changed. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 11: Verify AI Mode Change in Audit Trail

| Field | Detail |
|-------|--------|
| **Action** | Check the audit trail for AI mode configuration change events |
| **Command** | `curl -s "http://localhost:8080/api/admin/audit-trail?search=ai+mode" -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 2" \| python3 -m json.tool \| head -30` |
| **Expected Result** | Audit events showing the AI mode changes with change reasons, user attribution, and timestamps. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 12: Verify AI Mode in Frontend System Config

| Field | Detail |
|-------|--------|
| **Action** | Open System Configuration page in frontend and check AI Settings tab |
| **Command** | Navigate to http://localhost:3000/admin/system-config?tab=ai-settings |
| **Expected Result** | AI Settings tab displays current mode ("mock"). Mode selector or configuration controls visible. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

## Summary

| Metric | Value |
|--------|-------|
| **Total Steps** | 12 |
| **Passed** | |
| **Failed** | |
| **Blocked** | |
| **Pass Rate** | |

---

## Overall Result

| Field | Value |
|-------|-------|
| **Overall Status** | [ ] PASSED / [ ] FAILED / [ ] PASSED WITH DEVIATIONS |
| **Executed By** | |
| **Execution Date** | |
| **Review/Approval** | |
| **Review Date** | |

---

## Deviations & Observations

| # | Step | Deviation Description | Severity | Resolution |
|---|------|----------------------|----------|------------|
| | | | | |

---

## Attachments

- [ ] Screenshot of AI Settings tab in frontend
- [ ] Screenshot of vLLM status display
- [ ] Audit trail export showing AI mode changes

---

*End of Test Protocol TP-10.1.4*
