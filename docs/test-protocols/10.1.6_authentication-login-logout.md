# Test Protocol 10.1.6 — Authentication: Login/Logout

| Field | Value |
|-------|-------|
| **Protocol ID** | TP-10.1.6 |
| **Title** | Authentication — Login/Logout |
| **Version** | 1.0 |
| **Date Created** | 2026-06-13 |
| **Author** | hk |
| **Reviewer** | |
| **Objective** | Test login with valid/invalid credentials, session expiry behavior, logout clears session, and refresh token rotation |

---

## Prerequisites

| # | Prerequisite | Verified |
|---|-------------|----------|
| P1 | TP-10.1.2 passed (admin account exists and login works) | [ ] |
| P2 | Backend accessible at http://localhost:8080 | [ ] |
| P3 | Frontend accessible at http://localhost:3000 | [ ] |

---

## Test Environment

| Parameter | Value |
|-----------|-------|
| OS | |
| Backend URL | http://localhost:8080 |
| Frontend URL | http://localhost:3000 |

---

## Test Steps

### Step 1: Login with Valid Credentials

| Field | Detail |
|-------|--------|
| **Action** | Authenticate with correct admin credentials |
| **Command** | `curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"SecureP@ss2024!"}'` |
| **Expected Result** | HTTP 200 with JSON containing `access_token` and `user` object with id, username, email, full_name, roles. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 2: Login with Invalid Password

| Field | Detail |
|-------|--------|
| **Action** | Attempt login with wrong password |
| **Command** | `curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"WrongPassword"}'` |
| **Expected Result** | HTTP 401 with `{"detail":"Invalid credentials"}`. No token returned. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 3: Login with Non-Existent Username

| Field | Detail |
|-------|--------|
| **Action** | Attempt login with non-existent user |
| **Command** | `curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"nobody","password":"test123"}'` |
| **Expected Result** | HTTP 401 with `{"detail":"Invalid credentials"}`. Same error as wrong password (no username enumeration). |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 4: Login with Empty Credentials

| Field | Detail |
|-------|--------|
| **Action** | Attempt login with empty username and password |
| **Command** | `curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"","password":""}'` |
| **Expected Result** | HTTP 401 or 422 error. No token returned. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 5: Verify Token Contains User Info

| Field | Detail |
|-------|--------|
| **Action** | Login and inspect the returned user object |
| **Command** | `curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"SecureP@ss2024!"}' \| python3 -c "import sys,json; d=json.load(sys.stdin); u=d.get('user',{}); print(f'id={u.get(\"id\")} username={u.get(\"username\")} email={u.get(\"email\")} roles={u.get(\"roles\")}')"` |
| **Expected Result** | User object with id=1, username=admin, email=admin@alcoabase.dev, roles containing at least one role. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 6: Access Protected Endpoint with Valid Token

| Field | Detail |
|-------|--------|
| **Action** | Use the token to access a protected endpoint |
| **Command** | `TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"SecureP@ss2024!"}' \| python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])") && curl -s http://localhost:8080/api/v1/auth/me -H "Authorization: Bearer $TOKEN"` |
| **Expected Result** | HTTP 200 with user profile JSON (id, username, email, roles, companies). |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 7: Access Protected Endpoint with Invalid Token

| Field | Detail |
|-------|--------|
| **Action** | Attempt to access protected endpoint with a garbage token |
| **Command** | `curl -s http://localhost:8080/api/v1/auth/me -H "Authorization: Bearer invalid_token_garbage_12345"` |
| **Expected Result** | HTTP 401 Unauthorized. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 8: Access Protected Endpoint Without Token

| Field | Detail |
|-------|--------|
| **Action** | Attempt to access protected endpoint with no Authorization header |
| **Command** | `curl -s http://localhost:8080/api/v1/auth/me` |
| **Expected Result** | HTTP 401 Unauthorized. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 9: Logout via API

| Field | Detail |
|-------|--------|
| **Action** | Call the logout endpoint to invalidate the session |
| **Command** | `TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"SecureP@ss2024!"}' \| python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])") && curl -s -X POST http://localhost:8080/api/v1/auth/logout -H "Authorization: Bearer $TOKEN"` |
| **Expected Result** | HTTP 200 with confirmation message. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 10: Verify Token Refresh Endpoint Exists

| Field | Detail |
|-------|--------|
| **Action** | Call the refresh token endpoint |
| **Command** | `curl -s -X POST http://localhost:8080/api/v1/auth/refresh -H "Content-Type: application/json"` |
| **Expected Result** | HTTP 401 (no valid refresh token cookie) or HTTP 200 with new access_token (if refresh cookie was set). Endpoint exists and responds. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 11: Verify Deactivated User Cannot Login

| Field | Detail |
|-------|--------|
| **Action** | Attempt login with a deactivated user account |
| **Command** | First deactivate alc-user: `curl -s -X POST http://localhost:8080/api/admin/users/5/deactivate -H "Authorization: Bearer $TOKEN" -H "X-User-Id: 1" -H "X-Company-Id: 5" -H "X-Change-Reason: Test deactivation"` then attempt login: `curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"alc-user","password":"AlcCorp2024!"}'` |
| **Expected Result** | Login rejected — deactivated users cannot authenticate. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | |

---

### Step 12: Verify Frontend Login Flow

| Field | Detail |
|-------|--------|
| **Action** | Login via frontend UI, verify redirect to dashboard |
| **Command** | Open http://localhost:3000, enter admin/SecureP@ss2024!, click Login |
| **Expected Result** | Successful login, redirected to documents page. User name shown in header. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | Manual browser test. |

---

### Step 13: Verify Frontend Logout Flow

| Field | Detail |
|-------|--------|
| **Action** | Click the logout button in the header |
| **Command** | While logged in, click the LogOut icon in the top-right header |
| **Expected Result** | Redirected to login page. Refreshing the page stays on login (not auto-logged-in). |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | Manual browser test. |

---

### Step 14: Verify Session Expiry Behavior

| Field | Detail |
|-------|--------|
| **Action** | Use an expired token and verify rejection |
| **Command** | Use a previously obtained token after waiting beyond its TTL (or manually craft an expired JWT) and call: `curl -s http://localhost:8080/api/v1/auth/me -H "Authorization: Bearer $EXPIRED_TOKEN"` |
| **Expected Result** | HTTP 401 — expired tokens are rejected. |

| Result | |
|--------|---|
| **Status** | [ ] PASSED / [ ] FAILED / [ ] BLOCKED |
| **Actual Result** | |
| **Tester** | |
| **Date/Time** | |
| **Notes** | Manual test — requires waiting for token expiry or modifying token. |

---

## Summary

| Metric | Value |
|--------|-------|
| **Total Steps** | 14 |
| **Passed** | |
| **Failed** | |
| **Blocked** | |
| **Pass Rate** | |

---

## Overall Result

| Field | Value |
|-------|-------|
| **Overall Status** | [ ] PASSED / [ ] FAILED / [ ] PASSED WITH DEVIATIONS |
| **Executed By** | |
| **Execution Date** | |
| **Review/Approval** | |
| **Review Date** | |

---

## Deviations & Observations

| # | Step | Deviation Description | Severity | Resolution |
|---|------|----------------------|----------|------------|
| | | | | |

---

## Attachments

- [ ] Screenshot of login page
- [ ] Screenshot of successful login (dashboard)
- [ ] Screenshot of logout redirect

---

*End of Test Protocol TP-10.1.6*
